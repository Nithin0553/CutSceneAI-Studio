# Acceptance Tests

Repeatable manual gates live in `docs/acceptance` until they can be driven safely by dedicated
engine runners. Current gates cover live Dialogue v0.1 speech and Unreal Engine 5.8 portable-audio
import, restart persistence, and Movie Render Queue output.
