services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      APP_ENV: development
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      CUTSCENEAI_TTS_MODEL: ${CUTSCENEAI_TTS_MODEL:-gpt-4o-mini-tts}
