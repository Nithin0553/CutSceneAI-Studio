# Environment Agent

The future Environment Agent will propose scene dressing and environmental context as CIR changes.
It will not select or mutate engine assets directly. Asset Resolver v0.1 owns deterministic
project-index matching, and Unreal Adapter v0.7 owns the reviewed engine import plan.
