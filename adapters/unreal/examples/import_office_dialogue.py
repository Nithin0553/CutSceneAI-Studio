"""Generated by CutSceneAI Unreal Adapter v0.1 for Unreal Engine 5.8.0.

Run this file inside Unreal Editor with the Python Editor Script Plugin enabled.
Existing assets are never replaced: remove or rename a conflicting Level Sequence first.
"""

import json

import unreal


PLAN = json.loads('{"adapter_version":"0.1.0","cir_schema_version":"0.1.0","coordinate_system":{"distance_unit":"centimeter","forward_axis":"x","handedness":"left","position_mapping":"X=-Z*100; Y=X*100; Z=Y*100","up_axis":"z"},"preview_version":"0.1.0","project_id":"office-dialogue","project_name":"Office Dialogue","sequences":[{"actors":[{"actor_class_path":"/Script/Engine.Character","asset_path":null,"binding_id":"actor:mina","display_name":"ACT_Mina","kind":"character","placeholder":true,"source_entity_id":"mina","transform":{"location_cm":{"x":-100.0,"y":-150.0,"z":0.0},"rotation":{"w":1.0,"x":0.0,"y":0.0,"z":0.0},"scale":{"x":1.0,"y":1.0,"z":1.0}}},{"actor_class_path":"/Script/Engine.Character","asset_path":null,"binding_id":"actor:arjun","display_name":"ACT_Arjun","kind":"character","placeholder":true,"source_entity_id":"arjun","transform":{"location_cm":{"x":-50.0,"y":100.0,"z":0.0},"rotation":{"w":1.0,"x":0.0,"y":0.0,"z":0.0},"scale":{"x":1.0,"y":1.0,"z":1.0}}},{"actor_class_path":"/Script/Engine.StaticMeshActor","asset_path":null,"binding_id":"actor:contract","display_name":"ACT_Contract","kind":"environment","placeholder":true,"source_entity_id":"contract","transform":{"location_cm":{"x":0.0,"y":0.0,"z":80.0},"rotation":{"w":1.0,"x":0.0,"y":0.0,"z":0.0},"scale":{"x":1.0,"y":1.0,"z":1.0}}},{"actor_class_path":"/Script/Engine.StaticMeshActor","asset_path":null,"binding_id":"actor:conference-table","display_name":"ACT_ConferenceTable","kind":"environment","placeholder":true,"source_entity_id":"conference-table","transform":{"location_cm":{"x":0.0,"y":0.0,"z":0.0},"rotation":{"w":1.0,"x":0.0,"y":0.0,"z":0.0},"scale":{"x":1.0,"y":1.0,"z":1.0}}}],"asset_name":"LS_SceneMeeting","cameras":[{"angle":"eye_level","binding_id":"camera:shot-establishing","composition":"Slow push-in while preserving room geography.","description":"Wide view revealing the office, Mina, Arjun, and the table.","display_name":"CAM_ShotEstablishing","end_frame":96,"framing":"wide","inferred_transform":true,"lens_mm":28.0,"look_at_location_cm":{"x":-75.0,"y":-25.0,"z":160.0},"movement":"dolly","purpose":"establishing","source_beat_ids":["beat-arrival"],"source_shot_id":"shot-establishing","start_frame":0,"subject_binding_ids":["actor:mina","actor:arjun","actor:conference-table"],"target_binding_ids":["actor:mina","actor:arjun"],"transform":{"location_cm":{"x":-675.0,"y":-25.0,"z":160.0},"rotation":{"w":1.0,"x":0.0,"y":0.0,"z":0.0},"scale":{"x":1.0,"y":1.0,"z":1.0}}},{"angle":"high","binding_id":"camera:shot-contract-detail","composition":"Keep the empty signature line in the upper-right intersection.","description":"Insert shot revealing the unsigned signature line.","display_name":"CAM_ShotContractDetail","end_frame":144,"framing":"insert","inferred_transform":true,"lens_mm":70.0,"look_at_location_cm":{"x":0.0,"y":0.0,"z":80.0},"movement":"static","purpose":"environment_detail","source_beat_ids":["beat-confrontation"],"source_shot_id":"shot-contract-detail","start_frame":96,"subject_binding_ids":["actor:contract"],"target_binding_ids":["actor:contract"],"transform":{"location_cm":{"x":-120.0,"y":0.0,"z":240.0},"rotation":{"w":0.8944271909999159,"x":0.0,"y":0.4472135954999579,"z":0.0},"scale":{"x":1.0,"y":1.0,"z":1.0}}},{"angle":"eye_level","binding_id":"camera:shot-dialogue","composition":"Prioritize Mina while retaining Arjun in the foreground.","description":"Over-the-shoulder coverage of the confrontation.","display_name":"CAM_ShotDialogue","end_frame":336,"framing":"over_the_shoulder","inferred_transform":true,"lens_mm":50.0,"look_at_location_cm":{"x":-75.0,"y":-25.0,"z":160.0},"movement":"static","purpose":"dialogue","source_beat_ids":["beat-confrontation"],"source_shot_id":"shot-dialogue","start_frame":144,"subject_binding_ids":["actor:mina","actor:arjun"],"target_binding_ids":["actor:mina","actor:arjun"],"transform":{"location_cm":{"x":-325.0,"y":55.0,"z":160.0},"rotation":{"w":0.9880344496016635,"x":0.0,"y":0.0,"z":-0.15423335048016654},"scale":{"x":1.0,"y":1.0,"z":1.0}}},{"angle":"eye_level","binding_id":"camera:shot-reaction","composition":"Hold steady and preserve negative space toward Mina.","description":"Close-up of Arjun absorbing Mina\'s response.","display_name":"CAM_ShotReaction","end_frame":432,"framing":"close_up","inferred_transform":true,"lens_mm":85.0,"look_at_location_cm":{"x":-50.0,"y":100.0,"z":160.0},"movement":"static","purpose":"reaction","source_beat_ids":["beat-reaction"],"source_shot_id":"shot-reaction","start_frame":336,"subject_binding_ids":["actor:arjun"],"target_binding_ids":["actor:arjun"],"transform":{"location_cm":{"x":-230.0,"y":100.0,"z":160.0},"rotation":{"w":1.0,"x":0.0,"y":0.0,"z":0.0},"scale":{"x":1.0,"y":1.0,"z":1.0}}}],"duration_frames":432,"location":"Corporate conference room","package_path":"/Game/CutSceneAI/Sequences","performance_cues":[{"actor_binding_id":"actor:mina","dialogue":null,"dialogue_audio_uri":null,"dialogue_language":null,"dialogue_start_frame":null,"emotion":"frustrated","emotion_intensity":0.65,"end_frame":96,"facial_asset_uri":null,"lip_sync":false,"look_at_binding_id":"actor:contract","motion_asset_uri":null,"motion_prompt":"Walk briskly into the room, stop at the table, and look down at the contract.","motion_style":"restrained tension","source_beat_id":"beat-arrival","start_frame":0},{"actor_binding_id":"actor:mina","dialogue":"You said this would be signed yesterday.","dialogue_audio_uri":null,"dialogue_language":"en","dialogue_start_frame":120,"emotion":"angry","emotion_intensity":0.75,"end_frame":336,"facial_asset_uri":null,"lip_sync":true,"look_at_binding_id":"actor:arjun","motion_asset_uri":null,"motion_prompt":"Point toward the contract, then fold both arms while maintaining eye contact.","motion_style":"authoritative","source_beat_id":"beat-confrontation","start_frame":96},{"actor_binding_id":"actor:arjun","dialogue":"Legal changed the final clause. I was waiting for approval.","dialogue_audio_uri":null,"dialogue_language":"en","dialogue_start_frame":216,"emotion":"worried","emotion_intensity":0.7,"end_frame":336,"facial_asset_uri":null,"lip_sync":true,"look_at_binding_id":"actor:mina","motion_asset_uri":null,"motion_prompt":"Shift weight backward, glance at the contract, and raise one hand defensively.","motion_style":"uneasy","source_beat_id":"beat-confrontation","start_frame":96},{"actor_binding_id":"actor:arjun","dialogue":null,"dialogue_audio_uri":null,"dialogue_language":null,"dialogue_start_frame":null,"emotion":"anxious","emotion_intensity":0.8,"end_frame":432,"facial_asset_uri":null,"lip_sync":false,"look_at_binding_id":"actor:mina","motion_asset_uri":null,"motion_prompt":"Lower the raised hand slowly and become still.","motion_style":"defeated","source_beat_id":"beat-reaction","start_frame":336}],"source_scene_id":"scene-meeting","title":"The Meeting"}],"source_settings":{"distance_unit":"meter","forward_axis":"-z","fps":24,"handedness":"right","time_unit":"second","up_axis":"y"},"target_engine":"Unreal Engine","target_engine_version":"5.8.0","warnings":[{"code":"placeholder_character","message":"Character \'mina\' imports as an Unreal Character placeholder.","source_id":"mina"},{"code":"placeholder_character","message":"Character \'arjun\' imports as an Unreal Character placeholder.","source_id":"arjun"},{"code":"placeholder_environment","message":"Environment object \'contract\' imports as a StaticMeshActor placeholder.","source_id":"contract"},{"code":"placeholder_environment","message":"Environment object \'conference-table\' imports as a StaticMeshActor placeholder.","source_id":"conference-table"},{"code":"performance_metadata_only","message":"Performance cues are imported as editable Sequencer markers; animation and audio asset binding is a later adapter phase.","source_id":"scene-meeting"},{"code":"inferred_camera_transform","message":"Camera \'shot-establishing\' had no CIR transform; the adapter generated a deterministic blocking pose.","source_id":"shot-establishing"},{"code":"camera_movement_metadata_only","message":"Camera movement \'dolly\' is retained as metadata; v0.1 imports a blocking pose for manual keyframing.","source_id":"shot-establishing"},{"code":"inferred_camera_transform","message":"Camera \'shot-contract-detail\' had no CIR transform; the adapter generated a deterministic blocking pose.","source_id":"shot-contract-detail"},{"code":"inferred_camera_transform","message":"Camera \'shot-dialogue\' had no CIR transform; the adapter generated a deterministic blocking pose.","source_id":"shot-dialogue"},{"code":"inferred_camera_transform","message":"Camera \'shot-reaction\' had no CIR transform; the adapter generated a deterministic blocking pose.","source_id":"shot-reaction"}]}')
CAMERA_CLASS_PATH = "/Script/CinematicCamera.CineCameraActor"
PLACEHOLDER_CUBE_PATH = "/Engine/BasicShapes/Cube.Cube"


def _vector(data):
    return unreal.Vector(float(data["x"]), float(data["y"]), float(data["z"]))


def _rotator(data):
    return unreal.Quat(
        float(data["x"]),
        float(data["y"]),
        float(data["z"]),
        float(data["w"]),
    ).rotator()


def _set_transform(actor, data):
    actor.set_actor_location(_vector(data["location_cm"]), False, True)
    actor.set_actor_rotation(_rotator(data["rotation"]), True)
    actor.set_actor_scale3d(_vector(data["scale"]))


def _load_class(class_path):
    actor_class = unreal.load_class(None, class_path)
    if actor_class is None:
        raise RuntimeError(f"Unable to load Unreal class: {class_path}")
    return actor_class


def _add_actor(sequence, subsystem, actor):
    binding = subsystem.add_spawnable_from_class(
        sequence, _load_class(actor["actor_class_path"])
    )
    binding.set_display_name(actor["display_name"])
    template = binding.get_object_template()
    if template is None:
        raise RuntimeError(f"Spawnable has no object template: {actor['binding_id']}")
    template.set_actor_label(actor["display_name"])
    _set_transform(template, actor["transform"])

    if actor["kind"] == "environment":
        asset_path = actor["asset_path"] or PLACEHOLDER_CUBE_PATH
        static_mesh = unreal.EditorAssetLibrary.load_asset(asset_path)
        if isinstance(static_mesh, unreal.StaticMesh):
            component = template.get_editor_property("static_mesh_component")
            component.set_static_mesh(static_mesh)
        else:
            unreal.log_warning(f"Static mesh could not be loaded: {asset_path}")
    return binding


def _resolve_camera_actor(sequence, binding, camera):
    unreal.LevelSequenceEditorBlueprintLibrary.force_update()
    binding_id = sequence.get_binding_id(binding)
    bound_objects = unreal.LevelSequenceEditorBlueprintLibrary.get_bound_objects(binding_id)
    camera_actor = next(
        (
            item
            for item in bound_objects
            if isinstance(item, unreal.CineCameraActor)
        ),
        None,
    )
    if camera_actor is None:
        template = binding.get_object_template()
        if isinstance(template, unreal.CineCameraActor):
            camera_actor = template
    if camera_actor is None:
        raise RuntimeError(
            f"Unable to resolve spawned Cine Camera Actor: {camera['binding_id']}"
        )
    return camera_actor


def _set_camera_focal_length(component, lens_mm):
    lens_settings = component.get_editor_property("lens_settings")
    minimum = float(lens_settings.get_editor_property("min_focal_length"))
    maximum = float(lens_settings.get_editor_property("max_focal_length"))
    changed = False
    if lens_mm < minimum:
        lens_settings.set_editor_property("min_focal_length", lens_mm)
        changed = True
    if lens_mm > maximum:
        lens_settings.set_editor_property("max_focal_length", lens_mm)
        changed = True
    if changed:
        component.set_editor_property("lens_settings", lens_settings)
    component.set_editor_property("current_focal_length", lens_mm)


def _add_camera(sequence, subsystem, camera):
    binding = subsystem.add_spawnable_from_class(sequence, _load_class(CAMERA_CLASS_PATH))
    binding.set_display_name(camera["display_name"])
    camera_actor = _resolve_camera_actor(sequence, binding, camera)
    camera_actor.set_actor_label(camera["display_name"])
    _set_transform(camera_actor, camera["transform"])
    component = camera_actor.get_cine_camera_component()
    if component is None:
        raise RuntimeError(
            f"Spawned camera has no Cine Camera Component: {camera['binding_id']}"
        )
    _set_camera_focal_length(component, float(camera["lens_mm"]))
    subsystem.save_default_spawnable_state(binding)
    return binding


def _add_marker(sequence, frame, label, comment):
    marker = unreal.MovieSceneMarkedFrame(
        frame_number=unreal.FrameNumber(int(frame)), label=label
    )
    marker.set_editor_property("comment", comment)
    sequence.add_marked_frame_to_sequence(
        marker, unreal.MovieSceneTimeUnit.DISPLAY_RATE
    )


def _add_performance_markers(sequence, cues):
    for index, cue in enumerate(cues, start=1):
        actor_id = cue["actor_binding_id"].split(":", 1)[-1]
        label = f"PERF {index:02d} {actor_id} {cue['source_beat_id']}"
        comment = json.dumps(
            {
                "motion_prompt": cue["motion_prompt"],
                "motion_style": cue["motion_style"],
                "emotion": cue["emotion"],
                "emotion_intensity": cue["emotion_intensity"],
                "lip_sync": cue["lip_sync"],
                "look_at_binding_id": cue["look_at_binding_id"],
            },
            sort_keys=True,
        )
        _add_marker(sequence, cue["start_frame"], label, comment)
        if cue["dialogue"] is not None and cue["dialogue_start_frame"] is not None:
            _add_marker(
                sequence,
                cue["dialogue_start_frame"],
                f"DIALOGUE {index:02d} {actor_id}",
                cue["dialogue"],
            )


def _create_sequence(scene):
    asset_path = f"{scene['package_path']}/{scene['asset_name']}"
    if unreal.EditorAssetLibrary.does_asset_exist(asset_path):
        raise RuntimeError(
            f"Refusing to replace existing asset '{asset_path}'. "
            "Delete it intentionally or change the package path."
        )

    asset_tools = unreal.AssetToolsHelpers.get_asset_tools()
    sequence = asset_tools.create_asset(
        scene["asset_name"],
        scene["package_path"],
        unreal.LevelSequence,
        unreal.LevelSequenceFactoryNew(),
    )
    if sequence is None:
        raise RuntimeError(f"Unable to create Level Sequence: {asset_path}")

    sequence.set_display_rate(unreal.FrameRate(PLAN["source_settings"]["fps"], 1))
    sequence.set_playback_start(0)
    sequence.set_playback_end(int(scene["duration_frames"]))
    unreal.LevelSequenceEditorBlueprintLibrary.open_level_sequence(sequence)
    subsystem = unreal.get_editor_subsystem(unreal.LevelSequenceEditorSubsystem)

    bindings = {}
    for actor in scene["actors"]:
        bindings[actor["binding_id"]] = _add_actor(sequence, subsystem, actor)

    for camera in scene["cameras"]:
        bindings[camera["binding_id"]] = _add_camera(sequence, subsystem, camera)

    camera_cut_track = sequence.add_track(unreal.MovieSceneCameraCutTrack)
    for camera in scene["cameras"]:
        section = camera_cut_track.add_section()
        section.set_range(int(camera["start_frame"]), int(camera["end_frame"]))
        section.set_camera_binding_id(
            sequence.get_binding_id(bindings[camera["binding_id"]])
        )

    _add_performance_markers(sequence, scene["performance_cues"])
    sequence.sort_marked_frames()
    unreal.EditorAssetLibrary.save_loaded_asset(sequence, False)
    unreal.log(f"CutSceneAI created {asset_path}")
    return sequence


def import_plan():
    sequences = [_create_sequence(scene) for scene in PLAN["sequences"]]
    if sequences:
        unreal.LevelSequenceEditorBlueprintLibrary.open_level_sequence(sequences[-1])
    for warning in PLAN["warnings"]:
        unreal.log_warning(f"CutSceneAI [{warning['code']}]: {warning['message']}")
    return sequences


if __name__ == "__main__":
    import_plan()
