[project]
name = "cutsceneai-backend"
version = "0.1.0"
description = "Backend API for CutSceneAI Studio"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "uvicorn[standard]>=0.29.0"
]
