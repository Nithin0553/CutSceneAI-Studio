# CutSceneAI Preview

Preview v0.1 compiles validated CIR into an engine-neutral timeline manifest and renders the
same manifest as a deterministic SVG storyboard. It uses placeholders when production assets are
not yet available and makes no network or model calls.

Public artifacts:

- `schemas/preview-v0.1.schema.json`
- `examples/office-dialogue.preview.json`
